import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AudioplayPage } from './audioplay';

@NgModule({
  declarations: [
    AudioplayPage,
  ],
  imports: [
    IonicPageModule.forChild(AudioplayPage),
  ],
})
export class AudioplayPageModule {}
